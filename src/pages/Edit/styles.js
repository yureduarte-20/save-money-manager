import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container:{
        flex:1,
    },
    scrollView:{
        width:'100%',
        alignItems:'center',
        flexDirection:'row'
    }
})

export default styles